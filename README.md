# FR Owner WhatsApp Bot

## Termux setup:

pkg update && pkg upgrade -y
pkg install git nodejs ffmpeg imagemagick -y
git clone <YOUR-REPO-URL>
cd frbot
npm install
node index.js

Scan QR code with WhatsApp → Bot online
